<div class="footer-content">
    <div class="footer-menu">
        <NavLink href="/waterreminder" Match="NavLinkMatch.All" class="footer-link">
            <span class="menu-icon">💧</span>
            <span class="menu-text">Water Tracker</span>
        </NavLink>
    </div>
</div>

@code {
    protected override async Task OnInitializedAsync()
    {
        // Initialize if needed
    }
}

<style>
    .footer-content {
        max-width: 1400px;
        margin: 0 auto;
        width: 100%;
        display: flex;
        justify-content: space-between;
        align-items: center;
        gap: 2rem;
    }

    .footer-menu {
        display: flex;
        gap: 1rem;
        flex: 1;
    }

    .footer-link {
        display: inline-flex;
        align-items: center;
        gap: 0.5rem;
        padding: 0.5rem 1rem;
        color: rgba(255, 255, 255, 0.85);
        text-decoration: none;
        border-radius: 4px;
        transition: all 0.3s ease;
        font-size: 0.9rem;
        font-weight: 500;
    }

    .footer-link:hover {
        color: white;
        background: rgba(255, 255, 255, 0.15);
    }

    .footer-link.active {
        color: white;
        background: rgba(255, 255, 255, 0.2);
    }

    .menu-icon {
        font-size: 1rem;
        line-height: 1;
    }

    .menu-text {
        font-size: 0.9rem;
    }

    @@media (max-width: 768px) {
        .footer-content {
            flex-direction: column;
            gap: 0.75rem;
            align-items: flex-start;
        }

        .footer-menu {
            width: 100%;
            flex-wrap: wrap;
        }

        .footer-version {
            width: 100%;
        }
    }
</style>

